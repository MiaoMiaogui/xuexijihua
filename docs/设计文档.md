# 初高中学习计划与打卡 APP · 信息架构与页面流程图

> 配套工程：`../mobile-app/`（React Native）、`../backend/`（Node.js + MySQL + Redis）
> 设计原则：打卡操作 ≤ 1 次点击、计划制定 ≤ 3 步、护眼色系、家长/教师端不暴露学生细节。

---

## 一、信息架构图（Information Architecture）

```mermaid
graph TD
  App["学伴 App"] --> Role["身份选择<br/>学生 / 家长 / 老师"]
  App --> Tab["主框架 · 底部 TabBar"]

  Tab --> Home["🏠 首页<br/>今日任务 / 倒计时 / Streak / 进度环"]
  Tab --> Plan["📅 计划<br/>月/周日历 · 任务列表"]
  Tab --> CheckIn["✅ 打卡<br/>一键 / 拍照 / 时长"]
  Tab --> Stats["📊 统计<br/>热力图 / 占比 / 趋势 / 徽章"]
  Tab --> Profile["👤 我的<br/>账号 / 设置 / 协同"]

  Home --> AI["AI 规划 Banner → 计划向导"]
  Home --> Cal["→ 计划日历"]
  Plan --> Wizard["→ 计划向导(3步)"]
  Profile --> Focus["🎯 专注模式(番茄钟)"]
  Profile --> Wrong["📕 错题本/薄弱点"]
  Profile --> Collab["👨‍👩‍👦 家长/教师协同"]
  Profile --> Switch["切换身份 / 退出"]

  subgraph 学习科学层
    Wizard
  end
  subgraph 行为激励层
    CheckIn
    Focus
    Stats
  end
  subgraph 数据与反馈层
    Stats
    Wrong
  end
  subgraph 协同亮点
    Collab
  end
```

---

## 二、核心页面流程图（Page Flow）

```mermaid
flowchart TD
  Start(["打开 App"]) --> Role{"选择身份"}
  Role -->|学生| Home
  Role -->|家长| PV["家长协同视图<br/>时长/完成率/异常提醒"]
  Role -->|老师| TV["教师协同视图<br/>布置任务/班级数据"]

  Home["首页"] -->|勾选任务| Done["标记完成 · 进度环更新"]
  Home -->|点击 AI Banner| W1
  Home -->|点击日历卡片| Plan

  W1["计划向导 · 第1步<br/>目标/周期/科目"] --> W2["第2步<br/>任务类型/时长"]
  W2 --> W3["第3步<br/>AI 智能排课结果"]
  W3 -->|保存到日历| Plan

  Plan["计划/日历"] -->|点选某日| DayTasks["当日任务列表"]
  Plan -->|制定计划| W1

  CheckIn["打卡"] -->|一键打卡| StreakUp["连续打卡 +1 · 记录流水"]
  CheckIn -->|拍照/时长| Log["打卡记录"]

  Profile["我的"] -->|专注模式| Focus["番茄钟 25/5 循环"]
  Profile -->|错题本| Wrong["薄弱点列表"]
  Profile -->|协同| Collab["家长/教师视图"]
  Profile -->|退出| Start
```

---

## 三、前端页面 ↔ 后端 API 调用关系

```mermaid
flowchart LR
  Home -->|GET /api/tasks/today| BE[(Backend)]
  CheckIn -->|POST /api/checkins| BE
  PlanWizard -->|POST /api/plans/generate| BE
  Stats -->|GET /api/stats/overview| BE
  Wrong -->|GET/POST /api/wrong/weak-points| BE
  Profile -->|GET /api/exams| BE

  BE --> MySQL[("MySQL<br/>8 张核心表")]
  BE --> Redis[("Redis<br/>今日任务/连续打卡/周统计缓存")]
  BE -.->|计划推荐算法| Algo["planGenerator.ts<br/>目标倒推+艾宾浩斯+精力曲线"]
```

---

## 四、关键设计说明

1. **信息架构**：以「首页」为中枢，五大 Tab 覆盖方案基础层(MVP)到高阶能力；打卡作为底部中央 FAB，符合"≤1 次点击"原则。
2. **角色差异化**：学生端功能最全；家长/教师端为"只读协同视图"，仅暴露汇总指标与异常提醒，保护学生隐私（方案 2.5）。
3. **学习科学闭环**：计划向导(目标倒推) → 日历执行 → 打卡 → 统计反馈 → 错题再练，形成"计划-执行-反馈"闭环。
4. **激励与防 burnout**：连续打卡、成就徽章、学习热力图提供正向反馈；休息提醒与番茄钟防止虚假勤奋（方案 2.3）。
5. **性能**：高频读（今日任务、连续打卡、周统计）走 Redis 缓存，写操作后主动失效，降低 MySQL 压力（方案 2.4）。
